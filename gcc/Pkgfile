# Description: GNU Compiler Collection (C and C++ compilers)
# URL:         https://gcc.gnu.org/
# Depends on:  glibc-32 libmpc zstd

name=gcc
version=14.3.0
release=2
source="ftp://gcc.gnu.org/pub/gcc/releases/gcc-$version/gcc-$version.tar.xz
	gcc-4.7.3-multilib-dirs.patch
	gcc-configure-texinfo.patch
	gcc-nocheck-fixincludes.patch"

build() {
	for p in $SRC/*.patch; do
		patch -d gcc-$version -p1 -i $p
	done

	# Default libdir for 64-bit -> "lib"
	sed -i.orig '/m64=/s/lib64/lib/' \
		gcc-$version/gcc/config/i386/t-linux64

	# Remove -pipe (fails tests)
	export CFLAGS=$(echo $CFLAGS | sed 's/-pipe//')
	export CXXFLAGS=$(echo $CXXFLAGS | sed 's/-pipe//')

	mkdir build; cd build

	../gcc-$version/configure \
		--prefix=/usr \
		--libexecdir=/usr/lib \
		--disable-nls \
		--disable-fixincludes \
		--enable-__cxa_atexit \
		--enable-clocale=gnu \
		--enable-default-pie \
		--enable-default-ssp \
		--enable-languages=c,c++ \
		--enable-lto \
		--enable-multilib \
		--enable-plugin \
		--enable-shared \
		--enable-threads=posix \
		--with-system-zlib \
		--with-x=no \
		MAKEINFO=missing \

	make bootstrap
	make -j1 DESTDIR=$PKG install

	# Compatibility symlinks
	mkdir -p $PKG/lib
	ln -sf ../usr/bin/cpp $PKG/lib/cpp
	ln -sf gcc $PKG/usr/bin/cc
	ln -sf g++ $PKG/usr/bin/c++

	# Remove junk
	rm -r $PKG/usr/share/info \
		$PKG/usr/share/gcc-$version \
		$PKG/usr/bin/*-linux-gnu-* \
		$PKG/usr/lib/gcc/*/$version/install-tools \
		$PKG/usr/lib/gcc/*/$version/include-fixed \

	# Move gdb auto-load helpers
	for d in lib lib32; do
		mkdir -p $PKG/usr/share/gdb/auto-load/usr/$d
		mv $PKG/usr/$d/libstdc++.so.*-gdb.py \
			$PKG/usr/share/gdb/auto-load/usr/$d
	done

	# LTO plugin symlink
	mkdir -p $PKG/usr/lib/bfd-plugins
	ln -fsv ../../lib/gcc/$(
		$PKG/usr/bin/gcc -dumpmachine
	)/$version/liblto_plugin.so $PKG/usr/lib/bfd-plugins/

	# Clean up .la references
	sed -i "s|-L$SRC[^ ]* ||g" \
		$PKG/usr/lib/libstdc++.la $PKG/usr/lib32/libstdc++.la \
		$PKG/usr/lib/libsupc++.la $PKG/usr/lib32/libsupc++.la
}
