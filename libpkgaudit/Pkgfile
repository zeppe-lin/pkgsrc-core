# Description: Library for package integrity audits
# URL:         https://github.com/zeppe-lin/libpkgaudit
# Depends on:  scdoc

name=libpkgaudit
version=0.1.0-2738dd8 # keep in sync with pkgchk
release=1
source=https://github.com/zeppe-lin/$name/archive/${version#*-}/$name-$version.tar.gz

build() {
	for x in static shared; do
		meson setup build-$x $name-* \
			--prefix=/usr \
			--buildtype=plain \
			--wrap-mode=nodownload \
			-D default_library=$x \
			-D link_mode=$x \
			-D tests=enabled \
			-D tools=enabled \
			-D install_tools=false \
			-D man_pages=enabled \

		ninja -C build-$x -j ${JOBS:-1} -v
		meson test -C build-$x
		DESTDIR=$PKG ninja -C build-$x install
	done
}
