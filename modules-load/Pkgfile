# Description: Load kernel modules from configuration files
# URL:         https://github.com/zeppe-lin/modules-load
# Depends on:  kmod

name=modules-load
version=0.2-fdfef02
release=1
source=https://github.com/zeppe-lin/$name/archive/${version#*-}/$name-$version.tar.gz
build() {
	make -C $name-* PREFIX=/ MANPREFIX=/usr/share/man \
		BASHCOMPDIR=/usr/share/bash-completion/completions \
		DESTDIR=$PKG install
}
