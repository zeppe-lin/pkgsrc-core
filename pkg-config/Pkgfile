# Description: Managing library compile/link flags
# URL:         https://www.freedesktop.org/wiki/Software/pkg-config/

name=pkg-config
version=0.29.2
release=1
source=https://pkgconfig.freedesktop.org/releases/$name-$version.tar.gz

build() {
	mkdir build; cd build

	# --disable-host-tool: prevent creation of unwanted hard link
	# --with-internal-glib: use internal Glib, pkgsrc-core does not
	#   provide external
	../$name-$version/configure \
		--prefix=/usr \
		--disable-host-tool \
		--with-internal-glib \

	make V=1
	make DESTDIR=$PKG install

	# Remove junk
	rm -r $PKG/usr/share/doc
}
