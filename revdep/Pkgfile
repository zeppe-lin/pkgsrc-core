# Description: Check for missing or wrong libraries of installed packages
# URL:         https://github.com/zeppe-lin/revdep
# Depends on:  elfutils scdoc

name=revdep
version=5.0-8e8b22a
release=1
source=https://github.com/zeppe-lin/$name/archive/${version#*-}/$name-$version.tar.gz

build() {
	meson setup build $name-* \
		--prefix=/usr \
		--buildtype=plain \
		--wrap-mode=nodownload \
		-D b_lto=false \
		-D b_pie=true \
		-D link_mode=static \
		-D build_man=true \
		-D revdepd_dir=/etc/revdep.d \
		-D pkgdb_path=/var/lib/pkg/db \
		-D ldsoconf_path=/etc/ld.so.conf \

	ninja -C build -j ${JOBS:-1} -v
	DESTDIR=$PKG ninja -C build install
}
